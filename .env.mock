# Loaded by `npm run dev:mock` (vite --mode mock).
# Runs the app on generated fake data — no Supabase project needed.
VITE_USE_MOCK=true
# Simulated request latency in ms, so loading states stay visible.
VITE_MOCK_LATENCY=150
